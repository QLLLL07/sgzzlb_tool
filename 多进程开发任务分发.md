# 多进程开发任务分发

> 更新日期：2026-08-28  
> 当前协议版本：C API `0.2.0`（JSON 字符串返回）

## 目标与边界

本轮先固定核心库的进程边界，供 Qt、Python GUI、批量评测脚本和后续服务端分别接入：

```text
前端/批处理进程 ── ctypes / C ABI ──> libsgzzlb
                                          ├── 游戏基础数据（只读）
                                          ├── 蒙特卡洛战斗与配装推荐
                                          └── 本地账号内存快照 <──> account.json
```

- 每个进程必须各自调用 `load_data`，不要在进程之间传递 C++ 指针或返回字符串指针。
- 所有 `const char*` 返回值均由库分配，读取 JSON 后必须调用 `free_string`。
- `heroId` 是当前 `get_heroes()` 返回数组的 0-based 下标。重载不同版本数据后应重新查询，禁止将下标当作永久业务主键。
- 账号 API 只保存本地账号拥有的武将和 `stars`（0 到 5），不保存任何账号凭据或网络数据。
- `save_local_accounts` 写出完整快照；多进程同时编辑同一文件时，调用方必须在进程外做文件锁或单写者协调。原子落盘只保证文件不会半写，不能解决两个进程的最后写入覆盖。

## 已冻结接口

旧接口保持兼容：`load_data`、`reload_data`、`evaluate_team`、`evaluate_team_troop`、`recommend_teams`、`get_heroes`、`get_tactics`、`get_version`、`free_string`。

| 接口 | 入参 | 返回 JSON / 用途 |
|---|---|---|
| `get_tactic_max_level(name)` | 战法名 UTF-8 | 战法原始资料及满级模型：`level=10`、`numericMultiplier=2`。|
| `get_tactics_max_level()` | 无 | 全部战法的满级视图。|
| `evaluate_team_stars(id1,id2,id3,s1,s2,s3)` | 三名武将与 0-5 红度 | 按红度属性副本进行 200 场实战模拟的评估报告。|
| `create_local_account(name)` | 显示名称 | 新建账号，返回 `{ok,id,name,heroes}`。|
| `set_local_account_hero(accountId,heroId,stars,owned)` | `owned=1` 新增/更新；`0` 删除 | 更新账号拥有武将和红度，返回账号快照。|
| `get_local_account(accountId)` | 账号 ID | 单个账号快照。|
| `list_local_accounts()` | 无 | 账号数组。|
| `save_local_accounts(path)` | UTF-8 文件路径 | 将所有账号写入本地 JSON 文件。|
| `load_local_accounts(path)` | UTF-8 文件路径 | 导入本地 JSON 文件。|
| `recommend_account_teams(accountId,topN)` | 本地账号与数量 | 只枚举该账号拥有的武将，并把红度带入模拟。|
| `recommend_tactics(heroId,teammate1Id,teammate2Id,topN,sims)` | 固定一名武将和两名队友 | 逐个传承战法进行实战模拟，按胜率排序。|

### 数值约定

- **满级战法**：基础数据为 1 级文本数值。战斗引擎在 `TeamConfig.tacticLevel=10` 时将伤害、治疗、增伤、减伤、易伤、属性和发动率加成按 `1.0 -> 2.0` 线性外推；触发概率、目标数和回合数不变。该模型在接口结果中明确标记，后续可以由真实满级数据表替换。
- **红度**：`stars` 截断到 0-5。当前模型每红提升武力、智力、统率、速度的基础值和成长值各 2%，最高 10%；计算时生成副本，不修改全局原始数据。
- **战法推荐**：候选阵容先用规则分快筛，再为每个候选分配战法并对固定参考队做 200 场、8 回合蒙特卡洛。排序依据为 `70% 实战胜率 + 30% 规则分`；结果的 `evidence` 字段标识该依据。

## 账号文件格式

```json
{
  "version": 1,
  "accounts": [
    {
      "id": "local-1",
      "name": "我的账号",
      "heroes": [
        {"heroId": 17, "stars": 5, "name": "刘备"},
        {"heroId": 42, "stars": 2, "name": "关羽"}
      ]
    }
  ]
}
```

`name` 是显示字段，导入时以 `heroId` 为准；不存在的武将下标会被跳过。生产接入应在文件旁保留数据版本或英雄稳定 ID 的映射，以支持未来数据重排迁移。

## 并行任务拆分

| 任务 | 责任范围 | 依赖 | 验收标准 |
|---|---|---|---|
| A. 核心 API | `core/api.*`、`core/account.*` | 本文接口 | C ABI 不破坏旧函数；异常输入返回 `{ok:false,error}`。|
| B. 战斗校准 | `core/effects.*`、`core/battle.*` | 真实满级战法表 | 为每条已校准战法提供 1/10 级样本和回归对战结果。|
| C. 推荐实验 | `core/tactic_assign.*`、`core/recommend.*` | A、B | 为每次权重/配装变化提交固定 seed 的基线、胜率和耗时对比。|
| D. 前端账号页 | `app/qt/`、`app/main.py` | A | 可创建账号、勾选武将、选择红度、保存/载入并触发账号推荐。|
| E. 桥接与契约测试 | `app/core_bridge.py`、`tests/` | A | ctypes 签名齐全；账户 CRUD、红度评估、账号推荐、导入导出均覆盖。|
| F. 数据迁移 | `data/`、迁移脚本 | A | 新数据重排后能按稳定英雄 ID 迁移账号存档。|

## 协作规则

1. 每个任务只修改自身责任范围；跨边界接口变更先更新本文件和 `core/api.hpp`。
2. 推荐和战斗改动必须给出固定种子、模拟场次、样本阵容与预期差异，避免仅凭单次随机结果判断优劣。
3. 合并前运行 `make -j4`、`python3 tests/test_api.py` 和新增接口测试；改 CMake 后额外运行 `cmake --build build`。
4. 不提交 `libsgzzlb.so`、对象文件、`build/` 或个人账号 JSON；账号文件属于本地私有数据。
5. 一个进程负责写账号文件。需要多写者时，在宿主层实施锁或使用账号服务，不将进程内 `LocalAccount` 全局变量当作跨进程存储。
