# 《三国志·战略版》配将工具算法逻辑审查报告

> 初始审查日期：2026-08-28  
> 实现状态更新：2026-08-28（已按本报告完成 P0 修复及部分 P1/P2 改进；新增多参考队统计与置信区间）  
> 对应版本：`sgzzlb 0.4.0`  
> 审查范围：数据加载、Lv10 战法资料、属性和规则评分、自动配装、战斗模拟、蒙特卡洛推荐、账号武将与个人战法池。  
> 本文描述的是**当前代码实际行为**，并区分“可用于启发式筛选”和“可视为真实实战结论”的边界。

---

## 一、审查结论

当前项目可以作为以下用途的原型工具：

- 管理自己的武将、红度和传承战法库存；
- 浏览武将定位、兵种适性、战法资料和显式联动；
- 对候选阵容进行快速初筛；
- 在同一个近似模型内做粗粒度比较。

当前项目**不适合**直接作为真实胜率、T0/T1 强度、战法投入优先级或最终配队结论的依据。此前会系统性扭曲结果的 P0 状态机偏差已修复；目前的主要限制转为单一参考队、文本近似解析、贪心配装以及尚未完成的真实战报校准。

建议当前 UI 中的“胜率”和“综合分”理解为：

```text
对固定桃园枪参考队、在当前简化规则和文本解析模型下的启发式分数
```

而非游戏内真实胜率。

---

## 二、当前算法全景

```text
原始英雄/战法 JSON + Lv10 战法资料
  -> 数据加载、名称索引、资料挂载
  -> 属性计算、角色分类、兵种与规则评分
  -> 自动传承战法配装（或账号战法池限制）
  -> 固定参考队的 8 回合单场战斗
  -> 200 场蒙特卡洛汇总
  -> 70% 战斗胜率 + 30% 规则评分
  -> 全局推荐 / 账号限定推荐
```

| 层级 | 当前方法 | 用途 | 可信度结论 |
|---|---|---|---|
| 数据 | 138 武将、212 战法、125 条 Lv10 资料摘要 | 基础检索和展示 | 可用，资料覆盖并不等于可执行覆盖 |
| 属性 | 50 级基础/成长、适性、主将、国家队、红度 | 构建单位属性 | 可作为近似 |
| 定位 | 自带战法关键词 + 成长阈值 | 规则评分、自动配装 | 仅适合初筛 |
| 配装 | 角色契合分 + 固定联动分的贪心算法，含阵法/兵种唯一性 | 给 6 个传承槽填战法 | 启发式，不是全局最优 |
| 战斗 | 8 回合事件状态机，含施法者绑定、限时修正和伤兵守恒 | 估计相对强度 | P0 已修复，仍是近似模拟 |
| 推荐 | 规则预筛 + 主将枚举 + 并行蒙特卡洛 | Top-N 排序 | 仅代表“对固定参考队”的排序 |
| 账号 | 武将、红度、传承战法池约束 | 贴合个人库存 | 约束逻辑可用，战斗结论仍受上述问题影响 |

---

## 三、数据与满级战法逻辑

### 3.1 数据结构

- `data/data.json` 保存武将、战法及 `tacticMaxLevels`。
- 原始战法描述保留在 `Tactic::description`。
- Lv10 资料挂载到 `maxLevelDescription`、可靠性和版本冲突标记。
- 资料通过战法名称匹配；兵种“自带”变体会回退到基础名匹配。

### 3.2 可执行效果来源

战斗引擎有两条路径：

1. **专用定义表**：14 个精选战法直接写入可执行 Lv10 数值，例如盛气凌敌、横扫千军、燕人咆哮、风助火势、刮骨疗毒、暂避其锋、锋矢阵、八门金锁阵、虎豹骑、陷阵营、士别三日、奇计良谋等。
2. **通用文本解析**：其他战法按描述关键词推断伤害、治疗、状态、属性、增减伤等；数值型效果按 1 级到 10 级的线性倍率推演。

因此：

```text
有 Lv10 资料摘要 != 战斗引擎已精确实现该 Lv10 战法
```

大多数战法仍为文本近似。文本解析尤其难以可靠覆盖目标优先级、次数上限、冷却、触发条件、分担、规避、清除对象、特殊封印和版本分歧。

---

## 四、属性、兵种和规则评分

### 4.1 单位属性

默认使用 50 级：

```text
属性 = (基础属性 + 成长值 × 49) × 兵种适性倍率
```

附加规则：

- 主将四维各 `+10`；
- 三人同阵营，全队四维 `×1.10`；
- 适性：S=120%、A=100%、B=85%、C=70%；
- 红度不改四维，每红固定 `+3%` 造成伤害与 `-3%` 受到伤害；
- `evaluate_team_build` 支持每人最多 10 点自由属性。

### 4.2 自动兵种

自动兵种只比较骑、盾、弓、枪四类的全队适性均值，选择最高者。

```text
不考虑敌方兵种克制
不考虑兵种战法
不考虑实际战斗胜率
不考虑同适性时的体系偏好
```

因此自动兵种是便宜的默认值，不是最优兵种搜索。

### 4.3 角色分类

角色由自带战法描述关键词和成长阈值判定为：兵刃输出、谋略输出、坦克、治疗、控制或辅助。

优点：速度快、无需人工标签。  
限制：强依赖文案；多功能武将只能获得一个标签；很多核心队并不符合“输出 + 坦克 + 治疗”的传统结构。

### 4.4 规则评分

```text
规则分 =
  35% × 最佳兵种适性
+ 25% × 阵营一致性
+ 25% × 输出/坦克/治疗覆盖
+ 15% × 统御合规
```

- 三人同阵营：国家项 100；任意两人同阵营：50；否则 0。
- 统御超过 20：统御项为 0，规则总分再乘 0.2。
- 控制和辅助不直接提升角色覆盖分。

审查意见：规则分适合在大量三人组合中做快速预筛，不适合作为战力本身。它应该只占较低权重，且不能替代战斗模型的准确性。

---

## 五、自动配装逻辑

### 5.1 候选限制

每名武将最多装 2 个传承战法。候选战法需同时满足：

- 是传承战法且可参与战斗；
- 满足当前选择兵种；
- 不是该武将的自带战法或自身传承战法；
- 不与本队已分配的传承战法重名；
- 全队至多一个阵法、至多一个兵种战法（自带同类型战法也会占用该队伍级名额）；
- 账号推荐时，还必须属于账号战法池。

### 5.2 贪心分配

每轮在“未填满槽位的武将 × 可用战法”中，选最高分的组合，直到填满 6 个槽位或没有候选。

战法契合分来自：

- 角色与伤害/治疗/减伤/控制/净化/发动率等效果的匹配；
- 主动和突击的发动率；
- 少数硬编码联动加分。

已显式加分的联动包括：

```text
盛气凌敌 <-> 横扫千军
火烧连营 -> 风助火势
暂避其锋 <-> 刮骨疗毒
虎豹骑 <-> 一骑当千
```

### 5.3 现有限制

- 这是局部最优贪心，不保证 6 槽整体最优；
- 没有枚举战法组合、速度顺序和体系三段链；
- 账号战法池是集合，不记录同名战法副本数量；
- 手动评估可指定主将；全局与账号推荐会枚举三种主将位置，并将选中的主将置于返回阵容第一位；
- 仍不支持锁定武将、锁定战法、禁用战法等约束；
- 不会按真实目标规则、战法优先级和速度配速优化。

审查意见：配装算法目前可用作“推荐起点”，不应把自动配装视为最优答案。

---

## 六、战斗模拟逻辑（当前实现）

### 6.1 实际流程

单场最多 8 回合：

```text
准备阶段：被动 -> 阵法 -> 兵种 -> 指挥
每回合：计划效果 -> 按先攻/速度行动
单人行动：持续伤害 -> 主动 -> 普攻 -> 突击 -> 连击/反击
终止条件：任一主将死亡；8 回合无人死亡则平局
```

### 6.2 伤害模型

```text
属性差 = max(0, 武力 - 统率) 或 max(0, 智力 - 敌方智力)
基础伤害 = (兵力基础伤害 + 属性差 × 1.4375) × 1.6
最终伤害 = 保底伤害 + 战法伤害 × A类增伤 × B类受伤系数 × 兵种克制 × 浮动
```

- 伤害浮动：86%~94%；
- 兵种克制：枪→骑→盾→弓→枪，±15%；
- 主将受普攻时有 30% 集火概率；
- 减伤系数最低限制到 10%。

这套公式是社区实测近似，并非官方公开公式；作为统一比较基线可以接受，但必须通过实际战报校准。

---

## 七、P0 修复状态：已完成

以下 P0 项已在本次实现中修复，并由 `tests/test_api.py` 的接口回归覆盖主要输出契约。它们解决的是旧版本的系统性实现偏差，不代表战斗模型已经达到游戏服务端精度。

### P0-1 计划效果施法者绑定：已修复

`SchedEntry` 现在保存 `ownerSide`、`ownerIndex` 和 `sourceTactic`。回合开始时，计划效果只由其所属武将结算一次，不再由同阵营全部单位重复施放。

这使计划伤害、治疗、控制、主将条件及施法者属性能使用正确来源。

### P0-2 限时数值效果：已修复

单位增加 `Modifier` 列表，记录增伤、减伤、易伤、主动/突击发动率与属性变化的增量、属性维度和剩余回合。回合末会对已到期的修正回滚；无时长的准备阶段效果仍保留为永久效果。

当前实现采用“施放时写入数值、到期时反向回滚”的方式，而不是每次结算重算属性。对于目前不含复杂覆盖/驱散/层数规则的模型，该方式可以保证限时修正不会持续整场；复杂叠层规则仍属于后续工作。

### P0-3 控制门禁与 DoT 时机：已修复到当前状态集

- 计穷阻止主动战法判定；
- 缴械阻止普攻和突击；虚弱使伤害为 0；禁疗阻止治疗；震慑在 DoT 结算后跳过本回合行动；
- 混乱影响普攻和敌对战法目标，可随机命中存活的敌我单位；
- 遇袭会抵消下一次非必中伤害；必中可绕过遇袭；破阵在伤害属性差计算中忽略防御方统率/智力；
- 同类 DoT 重施时会刷新已保存的持续回合、伤害率和施法者属性。

“洞察”、伤害类型区分、驱散范围、次数上限等细节仍未结构化建模。

### P0-4 伤兵与死兵守恒：已修复

当前记账为：

```text
受伤：troops -= dmg；wounded += 0.9*dmg；dead += 0.1*dmg
伤兵转死：wounded -= converted；dead += converted
治疗：wounded -= heal；troops += heal
```

伤兵转死不再二次扣除 `troops`。伤害也会截断至目标剩余可行动兵力，避免击杀时把过量伤害计入统计。

### P0-5 场均输出/承伤方向：已修复

每个单位累计 `damageReceived`。单场结果统一输出：

```text
dmgDealt = 敌方实际受到的总伤害
dmgTaken = 我方实际受到的总伤害
```

---

## 八、蒙特卡洛与推荐逻辑

### 8.1 单队评估

- 默认对固定“桃园枪”参考队模拟 200 场；
- 参考队为刘备主将、关羽、张飞，带固定 6 个传承战法；
- 手动评估可选择三个槽位中的任一武将为主将；
- 平局按失败处理；
- 单队评估种子固定，结果可复现。

返回结果已增加：

- `drawRate`：8 回合内未分出主将胜负的比例；
- `winRateStdError`：以“胜利/非胜利”为 Bernoulli 试验计算的胜率标准误；
- `mainIdx` 与 `mainName`：手动评估的主将位置和名称。

因此 UI 的胜率应理解为：

```text
在当前模型下，对固定桃园枪参考队的 200 场胜率
```

不是赛季环境胜率。

### 8.2 全局推荐

```text
枚举 C(n, 3)
-> 过滤统御 > 20
-> 按快速规则分排序，取前 200
-> 自动配装
-> 枚举三种主将位置，各自进行 200 场模拟
-> 按 70% 胜率 + 30% 规则分排序
```

优点：速度快，适合 138 名武将的全局扫描。  
限制：

- 只对单一参考队优化；
- 使用贪心配装；
- 已输出平局率与胜率标准误，但尚未提供置信区间或对不同敌方的分项结果；
- 规则预筛可能提前丢弃实战上有潜力但不符合“国家/坦克/治疗”结构的队伍。

推荐返回的 `heroes[0]` 和 `mainHeroId` 都是已选中的主将，以便前端载入后保持同一构建。

### 8.3 固定队友战法推荐

该接口固定三名武将后，只给被测武将增加一个候选传承战法，并不补齐其他 5 个传承槽。因此它比较的是“单战法局部增量”，不是完整队伍的最终配装强度。

接口同样返回平局率与胜率标准误；这只描述该局部增量在固定桃园参考队下的抽样波动，不能补偿模型近似误差。

---

## 九、账号与个人战法池逻辑

账号当前保存：

- 账号 ID 与名称；
- 拥有武将及红度；
- 个人可配装传承战法池。

账号推荐流程：

```text
仅枚举已拥有武将
-> 红度写入候选队伍
-> 自动配装只从个人战法池选择
-> 对固定桃园枪模拟并排序
```

该库存约束本身是合理的，且比全局推荐更符合实际使用场景。

当前限制：

- 战法池是名称集合，不能表示同战法多份；
- 空战法池不会自动使用全局传承战法，只保留自带战法；
- 账号尚未记录武将等级、兵力、兵书、装备、宝物、特技、动态加点和队伍编组；
- 账号推荐仍受单一参考队和战斗模型近似影响，但已继承主将枚举、队伍级阵法/兵种唯一性及不确定性指标。

---

## 十、P1/P2：模型完善建议

### P1：部分已完成，剩余项目会显著提升结论质量

1. **已完成**：枚举或允许指定主将位置。
2. **已完成基础版本**：新增 `evaluate_team_references` 接口，当前提供桃园枪、魏法骑、蜀枪、吴骑四套固定参考队；输出每队胜率/平局率/标准误/95% 区间，并汇总平均胜率与最低胜率。参考队仍是人工固定模板，尚未覆盖完整赛季环境。
3. **部分完成**：已加入阵法、兵种战法队伍唯一性约束；事件战法和同战法副本数量仍未建模。
4. **部分完成**：已实现“损失兵力最多”治疗目标；属性最高/最低、敌方主将、复杂随机群体目标仍待补充。
5. 对已收录 Lv10 资料的战法逐条补可执行定义，而不是继续依赖关键词解析。
6. 将战法来源、触发条件、触发次数上限、冷却、叠层和可驱散性建模为结构化字段。
7. 用真实战报样本做模型参数校准，记录误差而不是只使用社区经验系数。

### P2：部分已完成，剩余产品与可解释性工作

1. **部分完成**：战法列表/API 已标记精确定义、Lv10 资料和关键词近似状态；战斗报告仍需更细粒度的逐战法来源提示。
2. **部分完成**：报告已展示主将；战法来源、自动兵种理由和评分证据仍可继续细化。
3. **已完成**：已显示平局率和标准误；战斗统计和推荐结果现返回起始随机种子及胜率 95% 置信区间，可复现同一批次模拟。
4. **部分完成**：已支持指定兵种、指定主将；锁定武将、锁定战法、禁用战法和多队共存仍未实现。
5. 账号扩展到多队、战法副本、装备、兵书、宝物与赛季配置。

---

## 十一、建议的修复顺序

```text
第一阶段：修正模拟正确性（已完成）
  1. 计划效果绑定施法者
  2. 可过期 Modifier 系统
  3. 控制门禁与目标选择
  4. 伤兵/死兵守恒与报告指标

第二阶段：验证模拟可信度（进行中）
  5. **已完成基础回归**：API 契约覆盖主将、平局率、标准误和队伍级战法唯一性；仍需补充不依赖随机种子的底层确定性测试
  6. 对桃园、吴骑、魏法、蜀枪等已知战报校准
  7. 输出多参考队结果及置信区间

第三阶段：提升配将质量（部分完成）
  8. **已完成主将枚举和基础队伍合法性约束**；仍需枚举/优化合法战法组合
  9. 逐条补高频战法可执行定义
 10. 再扩展兵书、装备、士气和多队共存
```

第一阶段 P0 修复已经完成，当前可以继续扩展数据和 UI，但新增战法定义仍应配套确定性测试。由于多参考队、真实战报校准和完整结构化战法语义尚未完成，推荐排名依旧只能作为固定参考队下的启发式结果。
