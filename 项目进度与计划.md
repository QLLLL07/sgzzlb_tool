# 《三国志·战略版》配将工具 —— 项目进度与计划

> 最后更新：2026-08-28
> 架构：C++17 动态库核心（`.so`/`.dll`）+ C++ Qt Widgets 原生 GUI；旧版 Python/tkinter 界面作为兼容入口保留。两个前端都通过 JSON C API 调用核心，**不使用命令行**。

---

## 一、当前进度总览

| 模块 | 状态 | 说明 |
|------|------|------|
| 环境准备 | ✅ 完成 | g++ 15.2、CMake 4.2、Qt 6.10.2；Qt Widgets 原生 GUI 已可构建 |
| 数据准备 | ✅ 完成 | 合并 `data.json`（138 武将 + 212 战法 + 125 条 Lv10 战法资料）已生成 |
| 内置回退数据 | ✅ 完成 | `builtin_fallback.hpp`（12 武将 + 25 战法） |
| C++ JSON 解析 | ✅ 完成 | `json.hpp`（解析 + 序列化，UTF-8 直通） |
| C++ 数据结构与加载 | ✅ 完成 | `data.hpp/cpp` |
| C++ 战法效果模型 | ✅ 完成 | `effects.hpp/cpp`（关键词抽取 + 17 个精选战法专用定义，14 个已校正为 Lv10） |
| C++ 战斗引擎 | ✅ 完成 | `battle.hpp/cpp`（8 回合蒙特卡洛、战损比、战法发动/伤害统计） |
| C++ 评分/角色/联动 | ✅ 完成 | `scoring.hpp/cpp`（多参考队战损评分 + 规则分） |
| C++ 战法配装 | ✅ 完成 | `tactic_assign.hpp/cpp`（贪心分配 + 联动加分；推荐阶段实战验证） |
| C++ 推荐搜索 | ✅ 完成 | `recommend.hpp/cpp`（多参考队战损比 + 两阶段并行 MC） |
| C++ API 导出 | ✅ 完成 | `api.hpp/api.cpp`（extern "C"，账号、战法池、多参考队评估、评估与推荐接口） |
| 构建系统 | ✅ 完成 | Makefile / CMake → `libsgzzlb.so` |
| Python 桥接 | ✅ 完成 | `core_bridge.py`（ctypes，串行化 + 自动释放） |
| Qt GUI | ✅ 完成 | `app/qt/main.cpp`（原生 Widgets，账号、武将红度、个人战法池与后台计算） |
| Python GUI | 兼容保留 | `main.py`（tkinter，供未安装 Qt 的环境继续使用） |
| 测试 / 文档 | ✅ 完成 | `tests/test_api.py`（账号武将/战法池/存档约束覆盖）、README、Qt 无显示环境启动检查 |

---

## 二、文件结构

```
/home/qin/sgzzlb_tool/
├── 三国志战略版_配将与战斗逻辑.md   # 需求逻辑文档（核心资料）
├── 项目进度与计划.md                # 本文档
├── data/
│   ├── heroes.json                 # 138 武将（原始）
│   ├── tactics.json                # 212 战法（原始）
│   ├── heroes.csv / tactics.csv    # 原始 CSV
│   ├── data.json                   # 【生成】合并数据 {heroes, tactics}（库加载用）
│   └── raw/                        # 原始数据备份
├── core/                           # C++ 动态库源码
│   ├── json.hpp                    # ✅ 极简 JSON DOM
│   ├── data.hpp / data.cpp         # ✅ Hero/Tactic/DataStore/加载/回退
│   ├── builtin_fallback.hpp        # ✅【生成】内置 12 武将回退
│   ├── effects.hpp / effects.cpp   # ✅ 战法→效果模型
│   ├── battle.hpp / battle.cpp     # ✅ 战斗引擎
│   ├── scoring.hpp / scoring.cpp   # ✅ 评分/角色/联动
│   ├── tactic_assign.hpp/.cpp      # ✅ 战法配装
│   ├── recommend.hpp/.cpp          # ✅ 推荐搜索
│   ├── api.hpp                     # ✅ C API 声明（Qt/Python 共用）
│   └── api.cpp                     # ✅ extern "C" 导出
├── app/
│   ├── gen_data.py                 # ✅ 生成 data.json + 回退头文件
│   ├── qt/main.cpp                  # ✅ Qt Widgets 原生 GUI
│   ├── core_bridge.py              # ✅ ctypes 桥接
│   └── main.py                     # 兼容：tkinter GUI
├── tests/
│   └── test_api.py                 # ✅ 冒烟测试
├── libsgzzlb.so                    # 【生成】动态库
└── README.md                       # ✅ 使用文档
```

---

## 三、已完成工作详情

### 1. 环境与决策（已定案）
- **pip 引导安装**：下载 `get-pip.py` → `python3 get-pip.py --user --break-system-packages`，装得 pip 26.2.1。
- **GUI 框架**：当前首选为 C++ Qt Widgets。核心以 C API 返回 JSON，Qt 直接链接核心库，不需要 Python/`ctypes`；`tkinter` 版本保留为兼容入口。
- 本机工具链：g++ 15.2（C++17 ✓）、cmake 4.2、make、python3 3.14、tkinter 8.6、Qt 6.10.2、18 核 CPU。

### 2. 数据
- `data.json` = `{"heroes":[138],"tactics":[212],"tacticMaxLevels":[125]}`，由 `app/gen_data.py` 从原始 JSON 与 `tactic_max_levels.json` 合并生成。
- `app/import_max_tactics.py` 从 `三国志战略版_满级战法数值大全.md` 生成结构化 Lv10 数据，保留名称、效果摘要、可靠性和版本冲突标记。125 条资料中有 72 个基础名可与当前战法表匹配（兵种自带变体共享基础名资料）。
- 关键数据事实（已验证）：
  - 每个武将的自带战法**都能在战法表按名查到**（0 缺失）→ 战斗引擎直接按名查战法，无类型问题。
  - 战法数值（如 燕人咆哮 52%、横扫千军 50%）**为 1 级数值**，满级约翻倍。
- 内置回退：`builtin_fallback.hpp` 内嵌 12 名将 + 25 战法 JSON，`loadDataFile` 失败时调用 `loadBuiltinFallback()` 兜底（满足"≥10 示例武将"要求）。

### 3. C++ 核心（已完成部分）
- **json.hpp**：`jq::Json` DOM（null/bool/number/string/array/object），解析含 `\uXXXX` 与代理对，序列化自动转义。
- **data**：`Hero`（四维 base/growth、兵种适性 S/A/B/C、自带/传承战法）、`Tactic`、全局 `DataStore`；`loadDataFromJson` / `loadDataFile` / `loadBuiltinFallback`。
- **effects**：把战法自由文本解析为可执行 `Effect` 列表：
  - 关键词抽取（伤害/治疗/状态/增伤/减伤/属性/发动率/先攻/连击/净化/反击/抵御）；
  - **括号感知**拆句、目标解析（敌军单体/群体/全体）、DoT（灼烧/中毒/水攻/溃逃）伤害率提取；
  - **17 个精选战法精确定义表**：盛气凌敌、横扫千军、燕人咆哮、火烧连营、风助火势、威震华夏、一骑当千、刮骨疗毒、暂避其锋、锋矢阵、八门金锁阵、虎豹骑、陷阵营、青囊、以逸待劳、士别三日、奇计良谋、勇者得前。
- **battle**：8 回合战斗模拟引擎：
  - 准备回合（被动→阵法→兵种→指挥，按速度）施放永久/计划效果；
  - 回合行动：状态结算(DoT) → 主动战法（含"准备 1 回合"蓄力） → 普攻（缴械跳过） → 突击 → 连击 → 反击；
  - 伤害公式按文档实现：兵力基础伤害分段表 + 属性差×1.4375×等级系数(1.6) + 保底伤害（B 类/克制/浮动 86~94%），减伤上限 90%；
  - 状态：计穷/缴械/震慑/混乱/虚弱/禁疗/灼烧/水攻/中毒/溃逃/先攻/连击；主将阵亡即败；
  - 伤兵机制：伤害 10% 死兵 + 90% 伤兵，回合末 10% 伤兵转死兵；
  - 兵种克制循环：枪→骑→盾→弓→枪（±15% 基础层）；
  - `simulateBattle` 蒙特卡洛（种子 + 偏移），支持按参考队聚合战损比，并统计每个战法的发动次数和伤害。

### 4. 已通过验证
- 全部核心 `.cpp` 通过 `g++ -std=c++17 -fsyntax-only`（无 error）。
- `make` 构建 `libsgzzlb.so` 成功；C API 现包含多参考队评估、置信区间与模拟种子字段。
- CMake 已构建 Qt 6 原生程序 `build/sgzzlb_qt`；`ldd` 确认 Qt6 与 `libsgzzlb.so` 依赖可解析，`QT_QPA_PLATFORM=offscreen` 启动检查通过。
- `tests/test_api.py` 冒烟测试全部通过（覆盖版本/加载/get_heroes/评估/多参考队/非法下标/recommend/回退/reload）。
- GUI 端到端驱动验证通过：加载 → 筛选 → 选将 → 评估 → 推荐 10 条 → 双击载入槽位 → 强队(桃园) 44.0 > 随机弱队 27.6。
- 战斗引擎对拍：桃园自战 ≈50%（对称）；弱队明显劣于强队；多线程推荐 3 次运行无段错误（~85ms）。
- 克制方向修正后：桃园枪 47.5% / 桃园骑 21.5%（枪克骑 ✓）。

---

## 四、本轮修复的问题（全部完成）

### scoring 遗留（2 处）
1. `bestTroopType` 形参 `const Hero* h[3]` → `const Hero* const h[3]`（`TeamConfig::hero` 为 const 数组成员，经 `const TeamConfig&` 传入时类型不匹配）。
2. `rs.role = covered * 100 / 3` 整数除法 → `(double)covered * 100.0 / 3.0`。

### 战斗引擎真 bug（本轮新发现并修复）
1. **阵营错乱**：`initUnit` 用 `(&tc == &A)` 地址比较判 side，`A` 是成员拷贝而 `tc` 是引用参数 → 全部单位 side=1，双方互攻 u[0]、胜者恒判 A。改为显式传入 side。**这是此前所有异常胜率的根因。**
2. **伤害记账错误**：`applyDamage` 只扣 10% 死兵而伤兵记 90%，`troops+wounded` 超过 `maxTroops`（负伤害、打不死人）。改为受伤全额扣兵力，10% 永久死兵 + 90% 转伤兵。
3. **兵种克制方向反了**：`counterFactor` 矩阵与逻辑文档相反（实现是"骑克枪"，文档是"枪克骑"）。改为文档循环 枪→骑→盾→弓→枪。
4. **多线程缓存竞态**：`fx()` / `analyzeCached` 静态缓存惰性填充，推荐阶段二并发首次解析会段错误。新增 `warmBattleCache()` / `warmTacticCache()` 在开线程前单线程预热。
5. 参考队补齐：张飞由 1 个战法补为 2 个（破军威胜），基线更公平。

---

## 五、本轮完成情况与后续规划（2026-08-28）

### 本轮已交付
1. ✅ 修复 `scoring.cpp` 两处遗留（`bestTroopType` 形参 const 类型、整数除法）。
2. ✅ `tactic_assign.hpp/cpp`：传承战法池（74 个）贪心分配，每将 2 个；同战法不跨将重复；兵种限定匹配；角色契合 + 联动加分（盛气凌敌→横扫千军、暂避其锋→刮骨疗毒、灼烧→风助火势 等）。
3. ✅ `recommend.hpp/cpp`：阶段一 C(138,3) 统御≤20 规则预筛 Top200；阶段二配装 + `std::thread` 并行蒙特卡洛（200 场×200 候选，18 线程，~85ms）取 Top-N。
4. ✅ `api.hpp/api.cpp`：`extern "C"` 导出接口覆盖加载、评估（含多参考队）、推荐、账号、战法资料和数据查询；字符串 malloc + `free_string` 释放；hero id = 数组下标。
5. ✅ 构建：`Makefile` + `CMakeLists.txt` → `libsgzzlb.so`（Windows `__declspec(dllexport)` 兼容宏已备）。
6. ✅ `core_bridge.py`：ctypes 封装（argtypes/restype、UTF-8 解码、锁串行化、调用后自动 free_string）。
7. ✅ `main.py`（tkinter）：筛选（阵营/定位/统御/搜索）→ 3 槽位选将 → 兵种选择 → 评估（评分分解/战斗统计/战法配装/联动/建议）→ 推荐 Top-N 表格（双击载入槽位）→ 数据重载；作为兼容入口保留。
8. ✅ `app/qt/main.cpp`（Qt Widgets）：完整迁移筛选、三槽位、兵种、评估报告、Top-10 推荐和双击回填；通过 `QThread` 执行 C++ 核心计算，主界面不会卡顿。CMake 自动探测 Qt 5/6 并生成 `sgzzlb_qt`。
9. ✅ `tests/test_api.py` 全部通过；Qt 6 原生程序完成构建、动态依赖检查和无显示环境启动检查。
10. ✅ 新增 `app/import_max_tactics.py` 与 `data/tactic_max_levels.json`：将 125 条满级资料结构化并合并到运行时数据。API 可返回资料摘要、可靠性与版本冲突；14 个精选战法已改为直接使用确认的 Lv10 可执行数值，避免线性倍率二次放大。
11. ✅ 新增 `evaluate_team_references` 多参考队评估接口：对当前可用的桃园枪、魏法骑、蜀枪、吴骑分别模拟，返回分项胜率/平局率/95% 区间/种子及平均与最低胜率；缺少参考队武将时自动跳过。
12. ✅ 所有战斗统计和推荐结果返回可复现的模拟起始种子及胜率 95% 置信区间，Qt/tk 前端评估文本同步展示。
13. ✅ 本地账号扩展到存储“拥有武将 + 0~5 红度 + 个人传承战法池”；存档版本升级为 v2，仍兼容旧版账号 JSON。`recommend_account_teams` 现在同时受账号武将、红度和战法池约束。
14. ✅ Qt 新增“我的账号”页：创建/切换账号、默认自动恢复/手动导入导出存档、搜索并维护武将与红度、搜索并维护个人传承战法池、账号限定 Top 10 推荐。每次账号编辑自动保存。
15. ✅ 核心评分改为多参考队战损模型：战损比为敌方承受伤害/我方承受伤害，战损评分参与综合评分并作为推荐主排序指标。
16. ✅ 战法推荐改为完整队伍逐项实战枚举；目标武将逐个替换候选战法，队友保留完整配装，结果按多参考队战损评分排序。
17. ✅ 战斗统计新增 `tacticStats`：具体战法平均发动次数、平均伤害，持续伤害归属到施放战法；相关字段同步到评估和推荐 API。

### 后续规划（未做，用户可择期）
- **战斗数值平衡调优**（用户已表示后续自行动手改）：
  - 为其余已收录 Lv10 资料补充可执行效果定义（当前 14 个精选战法直接使用满级值，其余仍从 1 级原值线性推演）；
  - 减伤堆叠上限/治疗强度、普攻爆头率（当前 30%）按实测回调，缓解"均衡续航队"胜率顶部压缩（92~99%）。
- **自动兵种改进**：`bestTroopType` 目前取"适性均值最高"，实战未必最优（如桃园适性平手时自动选盾，而手选枪更好）。可选改为"对参考队模拟各兵种取胜率最高"（代价 4× 计算量）。
- **推荐扩展**：支持"锁定某武将后推荐"（当前为全局 Top-N）；参考队可参数化。
- **数据增强**：武将/战法图片、兵书/装备/宝物的近似建模。
- **Windows 侧**：CMake 编译 `.dll`（`__declspec(dllexport)` 已兼容）；Qt Windows 发布需要 `windeployqt`，尚未在 Windows 主机实测。

---

## 六、已知近似 / 简化项（重要）

> 以下均为当前实现的有意简化，已在代码注释标注，请知悉：

1. **战法数值取数据原值（1 级）**，未做满级（10 级）放大。
   - 用户明确："先不管吧，后续我动手改"。已在 `effects.cpp` 注释注明，便于后续按满级数值替换。
2. **战斗公式多为玩家实测（🟡）**：
   - 治疗基数 420、等级系数 1.6、属性差系数 1.4375、伤害浮动 86~94% 等为近似。
3. **未建模**：兵书、装备、科技、宝物、士气、规避、警戒细分、副将牵制等机制（部分以减伤/免伤近似）。
4. **参考对手集合**：默认使用桃园枪、魏法骑、蜀枪、吴骑；缺少数据时自动跳过，按可用参考队平均战损比聚合。
5. **精选战法 17 个有专用执行定义**，其中 14 个已改用资料确认的 Lv10 数值；其余战法走关键词近似抽取，精度递减。
6. **自带战法按名查战法表**（已验证 0 缺失），若未来数据变化导致缺失需回退处理。
7. **主将阵亡即判负**（文档设定）+ 普攻 **30% 概率集火主将**（爆头倾向），使"主将承伤能力"显著影响胜率。
8. **自动兵种 = 适性均值最高**（`bestTroopType`），实战未必最优；GUI 已提供手选兵种。
9. **战损与战法统计**：`BattleStats` 返回平均战损比/战损评分，以及每个我方战法的平均发动次数和平均伤害；DoT 伤害归属施放战法。

---

## 七、需求原文要点（速查）

- C++ 核心：数据加载（`data.json` 不存在时用内置 ≥10 示例武将）、评分、推荐算法；`extern "C"` 导出。
- 兼容入口：Python `tkinter` 界面通过 `ctypes` 调用库函数；首选入口为 C++ Qt Widgets。
- 三个核心导出函数：`evaluate_team(int id1, int id2, int id3)` → JSON 评分报告；`recommend_teams(int top_n)` → JSON 数组；`free_string(const char*)` 释放内存；可选 `reload_data`。
